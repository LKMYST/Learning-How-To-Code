# Requriement:
# Find the shortest segment of number from a given number
# that sum of each digit is greater than target number

import re

number_list = re.findall(r"\d", input("Number: "))
target_sum = int(input("Target: "))

    